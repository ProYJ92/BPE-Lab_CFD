name: doc-check
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Verify AGENTS
        run: node verify_agent_doc.js
      - name: Tidy HTML
        run: tidy -q -errors *.html
